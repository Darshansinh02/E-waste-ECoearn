# Port configuration for the Express server
PORT=5000

# MongoDB Connection URI (Replace with MongoDB Atlas URI in production)
MONGO_URI=mongodb://localhost:27017/ecoearn

# JWT Signing Secret (Generate a strong random key in production)
JWT_SECRET=ecoearn_super_secret_jwt_sign_key_2026
